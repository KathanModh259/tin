{
  "version": 1,
  "assumptions": [
    "Public HTTPS site, safe GET only, no login or form submits.",
    "Report is evidence for fixes, not a penetration test or CVE audit."
  ],
  "effects": ["Writes reports/BUYER_TRUST.md in the bound project."],
  "cost_drivers": ["Deeper depth fetches more pages within the fenced GET budget."],
  "rubric": [
    {"id": "grounding", "question": "Does each claim cite its evidence URL and exact header or snippet?"},
    {"id": "actionability", "question": "Does each fix name the exact next file or setting with a copy-paste starter?"}
  ],
  "cases": [
    {
      "id": "ordinary",
      "description": "Standard trust audit of a public site.",
      "inputs": {"site_url": "https://example.com", "depth": "standard"},
      "expect": {
        "contains": ["Observed", "Fix now", "Verification record"],
        "excludes": ["CVE-", "exploitability"]
      }
    },
    {
      "id": "focused",
      "description": "Focused home plus checkout check with notes.",
      "inputs": {
        "site_url": "https://example.com",
        "depth": "focused",
        "notes": "Checkout lives at /checkout, guest option expected."
      },
      "expect": {
        "contains": ["Observed", "checkout", "Verification record"],
        "excludes": ["CVE-", "exploitability"]
      }
    },
    {
      "id": "extensive",
      "description": "Extensive audit including linked money pages.",
      "inputs": {"site_url": "https://example.com", "depth": "extensive"},
      "expect": {
        "contains": ["Observed", "Roadmap", "Verification record"],
        "excludes": ["CVE-", "exploitability"]
      }
    }
  ]
}
